[project]
name = "model-fleet-mcp"
version = "0.1.0"
description = "MCP server for delegating Anchor task specs to the local hardware fleet"
requires-python = ">=3.10"
dependencies = ["mcp[cli]>=1.2.0", "requests>=2.31", "PyYAML>=6.0"]
